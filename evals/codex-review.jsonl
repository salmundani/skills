{"skills": ["codex-review"], "query": "/codex-review", "expected_behavior": ["Determines the parent branch the current branch was based on rather than assuming main", "Runs codex exec review with that branch as --base, model gpt-5.6-sol, and reasoning effort high", "Triages each finding into fix or defer instead of applying all of them blindly"]}
{"skills": ["codex-review"], "query": "/codex-review", "expected_behavior": ["Commits the working tree before starting each review iteration", "Re-runs the review after applying fixes rather than stopping at the first pass", "Stops looping once the review is clean or every remaining finding has been deferred or rejected"]}
{"skills": ["codex-review"], "query": "/codex-review", "expected_behavior": ["States which findings were deferred or rejected and why, rather than silently dropping them", "Does not loop indefinitely when the same findings keep being reported and have already been rejected"]}
{"skills": ["codex-review"], "query": "/codex-review medium --branch develop", "expected_behavior": ["Passes model_reasoning_effort=medium from the positional argument", "Passes --base develop instead of detecting the parent branch", "Still uses the default model gpt-5.6-sol since --model was not given"]}
{"skills": ["codex-review"], "query": "/codex-review --model gpt-5.6-sol", "expected_behavior": ["Treats the leading --model flag as a flag, not as the effort value", "Falls back to reasoning effort high rather than passing model_reasoning_effort=--model", "Detects the parent branch since --branch was not given"]}
